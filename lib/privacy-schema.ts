import {db} from './server';
// Prepared statements keep complete trigger bodies intact; the hosting migration splitter does not.
export const privacyTriggers=["CREATE TRIGGER IF NOT EXISTS erase_learner BEFORE DELETE ON profiles BEGIN\n DELETE FROM squad_replies WHERE profile_id=OLD.id OR target_id IN (SELECT id FROM room_responses WHERE profile_id=OLD.id) OR room_code IN (SELECT code FROM rooms WHERE host=OLD.id);\n DELETE FROM room_responses WHERE profile_id=OLD.id OR room_code IN (SELECT code FROM rooms WHERE host=OLD.id);\n DELETE FROM members WHERE profile_id=OLD.id OR room_code IN (SELECT code FROM rooms WHERE host=OLD.id);\n UPDATE learning_state SET room_code=NULL,mode='solo' WHERE room_code IN (SELECT code FROM rooms WHERE host=OLD.id);\n DELETE FROM rooms WHERE host=OLD.id;\n DELETE FROM launch_reviews WHERE target_id IN (SELECT id FROM days WHERE profile_id=OLD.id) OR target_id IN (SELECT a.id FROM activities a JOIN days d ON a.day_id=d.id WHERE d.profile_id=OLD.id);\n DELETE FROM activities WHERE day_id IN (SELECT id FROM days WHERE profile_id=OLD.id);\n DELETE FROM days WHERE profile_id=OLD.id;\n DELETE FROM evidence WHERE profile_id=OLD.id;\n DELETE FROM drafts WHERE profile_id=OLD.id;\n DELETE FROM events WHERE profile_id=OLD.id;\n DELETE FROM family_journal WHERE profile_id=OLD.id;\n DELETE FROM family_setups WHERE profile_id=OLD.id;\n DELETE FROM help_requests WHERE profile_id=OLD.id;\n DELETE FROM learner_preferences WHERE profile_id=OLD.id;\n DELETE FROM learner_usage WHERE profile_id=OLD.id;\n DELETE FROM learning_state WHERE profile_id=OLD.id;\n DELETE FROM learning_years WHERE profile_id=OLD.id;\n DELETE FROM school_records WHERE profile_id=OLD.id;\n DELETE FROM account_learners WHERE profile_id=OLD.id;\n DELETE FROM device_codes WHERE profile_id=OLD.id;\n DELETE FROM learner_lifecycle WHERE profile_id=OLD.id;\n DELETE FROM settings WHERE id LIKE 'family_first_learner:%' AND value=OLD.id;\nEND;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_evidence BEFORE INSERT ON evidence WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_drafts BEFORE INSERT ON drafts WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_events BEFORE INSERT ON events WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_family_journal BEFORE INSERT ON family_journal WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_family_setups BEFORE INSERT ON family_setups WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_help_requests BEFORE INSERT ON help_requests WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_learner_preferences BEFORE INSERT ON learner_preferences WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_learner_usage BEFORE INSERT ON learner_usage WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_learning_state BEFORE INSERT ON learning_state WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_learning_years BEFORE INSERT ON learning_years WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_school_records BEFORE INSERT ON school_records WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_account_learners BEFORE INSERT ON account_learners WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_device_codes BEFORE INSERT ON device_codes WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_learner_lifecycle BEFORE INSERT ON learner_lifecycle WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_days BEFORE INSERT ON days WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_room_responses BEFORE INSERT ON room_responses WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_squad_replies BEFORE INSERT ON squad_replies WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_members BEFORE INSERT ON members WHEN NEW.profile_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.profile_id) BEGIN SELECT RAISE(ABORT,'Learner no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_activity BEFORE INSERT ON activities WHEN NOT EXISTS(SELECT 1 FROM days WHERE id=NEW.day_id) BEGIN SELECT RAISE(ABORT,'Day no longer exists'); END;", "CREATE TRIGGER IF NOT EXISTS start_learner_retention AFTER INSERT ON profiles BEGIN\n INSERT INTO learner_lifecycle(profile_id,expires_at) VALUES(NEW.id,CAST(strftime('%s','now') AS INTEGER)*1000+31536000000);\nEND;", "CREATE TRIGGER IF NOT EXISTS prevent_orphan_room BEFORE INSERT ON rooms BEGIN SELECT CASE WHEN NOT EXISTS(SELECT 1 FROM profiles WHERE id=NEW.host) THEN RAISE(ABORT, 'Learner deleted') END; END;"];
let ready=false;
export async function ensurePrivacySchema(){if(ready)return;await db().batch(privacyTriggers.map(sql=>db().prepare(sql)));ready=true;}
