'use client';
import {useEffect,useState} from 'react';
import {api} from './client-api';
export function ServiceOperationsPanel(){const [data,setData]=useState<any>(null),[error,setError]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{api('service-status').then(setData).catch(e=>setError(e.message));},[]);return <section className="workspace-card"><h3>Automatic service recovery</h3><p>Payment events update access. App activity retries pending billing notices and removes expired learner records. There is no independent background timer.</p>{error&&<p role="alert">{error}</p>}{data&&<><p>Email sender: {data.emailReady?'enabled':'needs connection'} · Pending parental verification: {data.pendingConsent} · Payment errors: {data.paymentErrors}</p><p>Last maintenance: {data.lastRun||'not recorded'}</p><p>Search notification: {data.search?.status||'not yet submitted'}. A submitted URL is not evidence of indexing or visitors.</p>{data.search?.checkedAt&&<p>Last search submission: {data.search.checkedAt}</p>}<a href="/family-privacy">Review parental verification requests</a>{data.notices.map((n:any)=><p key={n.id}>{n.kind} · {n.sent_at?'accepted by provider':n.error?.startsWith('Needs attention:')?'needs attention':'queued'} · {n.attempts} attempts{n.error?' · '+n.error:''}</p>)}</>}<p>Email acceptance does not confirm inbox delivery.</p><button disabled={busy} onClick={async()=>{setBusy(true);setError('');try{setData(await api('service-run',{}));}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}>Run recovery and refresh</button></section>}
